import Popup from './Popup.js'

export default class AvatarPopup extends Popup {
  constructor (popupSelector) {
    super(popupSelector)
  }
  open() {
    super.open()
  }
  setSubmitHandler() {

  }
}
